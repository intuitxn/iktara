---
description: Set up and run the local app on port 3220
agent: iktara-builder
---
Run `npm run setup` when dependencies changed, then `npm run dev`. The local app serves on http://127.0.0.1:3220 with the chart service on 8020; do not touch the deployed app on 3210/8001. Use synthetic birth profiles for any browser verification.
