---
description: Produce the revisioned project source map
agent: iktara-builder
---
Run `npm run project:context` and show the JSON source map. Confirm every listed context file exists, note the checkout revision and dirty flag, and use the map when handing work off to Telepathy or another agent. Never include private runtime data.
