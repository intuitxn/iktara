# Copy to .env.local; never commit real keys.
# Provider/model ID supported by your OpenCode account.
OPENCODE_MODEL=opencode/deepseek-v4-flash
OPENCODE_API_KEY=
# Set when a named public tunnel is connected (not needed for local development).
# IKTARA_PUBLIC_ORIGIN=https://forsee.life
